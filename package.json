{
  "name": "blinkit-auto",
  "version": "0.1.0",
  "private": true,
  "description": "Safe local-first automation architecture for Blinkit-Auto",
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit",
    "start": "tsx src/index.ts",
    "test": "vitest run --pool=threads --maxWorkers=1"
  },
  "engines": {
    "node": ">=24"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "tsx": "^4.20.0",
    "typescript": "^5.8.0",
    "vitest": "^3.2.0"
  }
}
