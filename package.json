{
  "name": "blinkit-auto",
  "version": "0.1.1",
  "description": "Safe local-first automation architecture for Blinkit-Auto",
  "type": "module",
  "bin": {
    "blinkit-auto": "bin/blinkit-auto.js"
  },
  "files": [
    "dist/src",
    "bin",
    "README.md"
  ],
  "scripts": {
    "build": "tsc",
    "start": "tsx src/index.ts",
    "test": "vitest run --pool=threads --maxWorkers=1"
  },
  "engines": {
    "node": ">=24"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "@types/prompts": "^2.4.9",
    "tsx": "^4.20.0",
    "typescript": "^5.8.0",
    "vitest": "^3.2.0"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "prompts": "^2.4.2"
  }
}
